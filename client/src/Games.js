import React from 'react';

const Games = (props) => {
    const { data } = props;

    if (!data)
        return <div></div>;

    return (
        <div></div>
    );
};

export default Games;